<div class="container ">
  <div class="row justify-content-center">
    <div class="w-50  justify-content-center">
      <div class="row　border  justify-content-center pt-4 ">
        <div class="bg-primary text-center text-white"><%= @job.name %></div>
      </div>
      <div class="row　border justify-content-center">
        <div class="bg-white text-center text-black"><%= @theme.name %></div>
      </div>
    </div>
  </div>
  <div id="comments_area" class="justify-content-center  py-4 ">
    <% @comments.each do |comment| %>
      <!--<div class="col-sm-10">-->
        <div id="comments_area_<%=comment.id%>">
          <%= render partial: 'public/comments/comments', locals:{job: @job, theme: @theme,comment: comment} %>
        </div>
      <!--</div>-->
      <!--<div class="col-sm-2">-->
      <!--  <div class="row justify-content-center">-->
      <!--    <%#= image_tag comment.member.get_profile_image(70,70) %>-->
      <!--  </div>-->
      <!--  <div class="row justify-content-center">-->
      <!--    <%#= link_to member_path(comment.member) do %>-->
      <!--      <%#= comment.member.nickname %>-->
      <!--    <%# end %>-->
      <!--  </div>-->
      <!--</div>-->
      <!--<div class="col-sm-7">-->
      <!--  <div class="comment-all">-->
      <!--  <%#= comment.comment %>-->
      <!--  </div>-->
      <!--</div>-->
    <% end %>
  </div>
  <div class="row justify-content-center ">
    <div id="comments_new">
      <%= render partial: 'public/comments/form', locals:{job: @job, theme: @theme, comment: @comment} %>
    </div>
    <%# if current_member.email != 'guest@example.com' || admin_signed_in? %>
      <%#= form_with model: [@theme, @comment], url:job_theme_comments_path(@job,@theme), local: false do |f| %>
      <!--コメント入力フォーム<br>-->
      <%#= f.text_area :comment, rows: '3', cols: '70',placeholder: "コメントをこちらに" %>
      <%#= f.submit "送信" ,  class: "col-sm-5 "%>
      <%# end %>
    <%# end %>
    </div>
  </div>
  <div class="row justify-content-center py-4">
      <%= link_to "テーマ一覧へ戻る" , job_path(@job), class: 'btn btn-primary col-sm-5' %>
  </div>
</div>