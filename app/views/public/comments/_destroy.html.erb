<% if comment.member == current_member || admin_signed_in? %>
  <%= link_to  job_theme_comment_path(@job ,@theme ,comment), method: :delete, remote: true,  data: { confirm: '本当に消しますか？' },  class: "btn btn-danger" do %>
    <i class="fas fa-trash-alt"></i>
  <% end %>
<% end %>