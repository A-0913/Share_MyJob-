<div class = "container">
  <div class = "row justify-content-center pt-3">
    <h3><%= @member.nickname %>さんの職業投稿履歴</h3>
  </div>
    <% @jobs.each do |job| %>
      <div class="row justify-content-center py-3">
        <div class="row col-10 justify-content-center">
          <div class="col-md-5 text-center border border-white bg-info text-white">投稿日</div>
          <div class="col-md-5 text-center border border-dark bg-white text-dark"><%= job.created_at.strftime('%Y/%m/%d') %></div>
        </div>
        <div class="row col-10 justify-content-center">
          <div class="col-md-5 text-center border border-white bg-info text-white">職業ジャンル</div>
          <div class="col-md-5 text-center border border-dark bg-white text-dark"><%= job.genre.name %></div>
        </div>
        <div class="row col-10 justify-content-center">
          <div class="col-md-5 text-center border border-white bg-info text-white">職業名</div>
          <div class="col-md-5 text-center border border-dark bg-white text-dark"><%= job.name %></div>
        </div>
        <div class="row col-10 justify-content-center">
          <div class="col-md-5 text-center border border-white bg-info text-white">追加理由</div>
          <div class="col-md-5 text-center border border-dark bg-white text-dark"><%= job.reason %></div>
        </div>
        <div class="row col-10 justify-content-center">
          <div class="col-md-5 text-center border border-white bg-info text-white">公開/非公開</div>
            <div class="col-md-5 text-center border border-dark bg-white text-dark">
              <% if job.is_published == false %>
                <div class="text-danger font-weight-bold">非公開</div>
              <% else %>
                <div class="text-success font-weight-bold">公開</div>
              <% end %>
            </div>
        </div>
        <div class="row col-10 justify-content-center">
          <div class="col-md-5 text-center border border-white bg-info text-white">管理者確認</div>
            <div class="col-md-5 text-center border border-dark bg-white text-dark">
              <% if job.is_checked == false %>
                <div class="text-danger font-weight-bold">未確認</div>
              <% else %>
                <div class="text-success font-weight-bold">確認済み</div>
              <% end %>
            </div>
        </div>
      </div>
    <% end %>


  <div class="row justify-content-center">
    <%= paginate @jobs %>
  </div>

  <div class="row justify-content-center py-4">
    <%= link_to "プロフィールへ戻る" , admin_member_path(@member) , class: "btn btn-primary col-sm-3" %>
  </div>
</div>