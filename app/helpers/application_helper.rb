module ApplicationHelper
  # def find_page(theme,job,comment)
  #   comments = Comment.where(theme_id: theme.id).where(job_id: job.id).where(is_published: true).order(created_at: :desc)
  #   order_num = comments.index(comment) + 1
  #   order_num / 7
  # end
end
